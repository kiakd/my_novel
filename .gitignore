# secrets
.env
.env.local
*.local
# ทุก env variant (.env.atlas, .env.production ฯลฯ)
.env.*
# ยกเว้น template (ไฟล์ลงท้าย .example)
!*.example
# ไฟล์ backup (มัก copy มาจาก .env จริง)
*.bak
# mongodb dump ที่ดึงจาก server (อย่า commit ข้อมูลจริง)
backup-novel-*.gz

# deps / build
node_modules/
dist/
.next/
*.log

# python / comfyui runtime
venv/
__pycache__/
*.pyc
# ComfyUI = clone third-party (embedded git repo) ติดตั้ง local ลงใหม่ได้ตาม comfyui/README.md
comfyui/ComfyUI/
# ไฟล์ว่างพิมพ์พลาด
comfyui/textToImagePromp

# models (โหลดใหม่จาก Civitai)
*.safetensors
*.ckpt
*.pt
*.pth

# images / media (exclude ทั้งหมด)
*.png
*.jpg
*.jpeg
*.webp
*.gif
*.mp4
uploads/
ref/
output/

# os / editor
.DS_Store
.claude/

# RAG long-term memory store (runtime data — อย่า commit)
chat-mem.sqlite*
