# เสียง (audio) — ออปชัน เติมทีหลังได้

- เพลงประกอบ: วางไฟล์ `.ogg` ใน `bgm/` เช่น `bgm/main_theme.ogg` → เรียกด้วย `AudioManager.play_bgm("main_theme")`
- เสียงเอฟเฟกต์: วางไฟล์ `.ogg` ใน `sfx/` เช่น `sfx/impact.ogg` → `AudioManager.play_sfx("impact")`

Phase 1 ไม่บังคับ — ระบบเป็น stub ไว้แล้ว ไม่มีไฟล์ก็เล่นได้ปกติ
